---
date: 2025-07-19
tags:
  - 学习
---
# 自定义函数
1. 基础格式
>[!success]- right
>```Golang
>func cal(a int, b int) (int,int) {//若返回值只有一个类型，可以省略括号
>return a + b,a * b
>}
>
>sum,result := cal(num1,num2)
>sum,_ := cal(num1,num2) //如果不需要第二个返回值，可以用_代替

- 函数与函数之间为并列关系，可以直接调用，可以写在main函数以后
- 没有返回值就可以不写返回类型
>[!success]- example
>```Golang
>func test3(num int) {
    fmt.Println(num)
}
- 可以将函数赋值给变量，通过变量对函数进行调用
>[!success]- example
>a := test3
    a(10)//通过该变量对函数进行调用
2. 传入任意个数参数
>[!success]- args...int
>```Golang
>func test(args...int) {
>for i := 0;i < len(args);i++ {
>	fmt.Println(args[i])
>	}
>}
>
>test(1,2,3,4,5,6)

3. 通过指针修改值
![[Pasted image 20250725162638.png]]
>[!success]- example
>```Golang
>var num5 int = 10
    test2(&num5)
    fmt.Println("num5:", num5)
3. 定义一个函数，把另一个函数作为参数传入
>[!success]- example
>```Golang
>func test01(num1 int,num2 int, testFunc func(int) ) {
    fmt.Println("done")
}
test01(1,2,test3)
4. 自定义函数类型
>[!success]- example
>```Golang
>type MyFunc func(int)//自定义函数类型
func test02(num1 int,num2 int, testFunc MyFunc ) {
    fmt.Println("done")
}
5. 省略返回值
>[!success]- example
>```Golang
>func test03(num1 int,num2 int) (sum int,sub int) {
    sum = num1 + num2
    sub = num1 - num2
    return//可以省略返回值
}
p,m := test03(10,5)
6. 自定义类型
>[!success]- example
>```Golang
>type MyInt int
    var num6 MyInt = 10
    fmt.Println(num6)
- 虽然是自定义类型，但仍然是int类型，且编译识别时认为int 与 MyInt 不是一种类型
# 包的引入
>[!success]- main.go
>```Golang
>package main
import "fmt"
import "gocode/project01/unit5/crm/dbutils"
func main() {
    fmt.Println("Hello, world!")
    dbutils.Connection()
 }//尚未解决

在main.go中导入dbutils.go里的Connection函数(大写开头才可以被导用)
>[!success]- dbutils.go
>```Golang
>package dbutils
import "fmt"
func Connection() {
    fmt.Println("Connecting to database...")
}

导入包名为所在目录名![[Pasted image 20250725170756.png]]
 ==还未解决== ==还未解决== ==还未解决== ==还未解决== ==还未解决== ==还未解决== ==还未解决== ==还未解决==

# 其他函数
1. 全局变量的定义
>[!success]- example
>```Golang
>var num int = test()//全局变量的定义
func test() int {
    fmt.Println("test function")
    return 10
}
2. init函数定义
>[!success]- init函数
>```Golang
>func init() {//init函数定义 
>fmt.Println("init function")
}
- 初始化函数，用于进行一些初始化的操作，会在main函数之前执行
3. 匿名函数
>[!success]- example
>```Golang
>result := func(num1 int ,num2 int) int{//只使用一次
>       fmt.Println("anonymous function")
>       return num1 + num2
}(10,20)
fmt.Println(result)
- 在函数结尾自带数值
4. 闭包
- 闭包，类似递归操作。本质时一个匿名函数，只是引入外界的变量
- 匿名函数 + 引用的变量 = 闭包
- 闭包的特点：函数嵌套，函数引用了外部变量，返回一个函数,不清除内存
>[!success]- example
>```Golang
>func getSum() func(int int {
>	var sum int = 0
    return func (num int) int {
        sum += num
        return sum
    }
>}
>
>f := getSum()
>    fmt.Println(f(10))
>     fmt.Println(f(20))//每次调用都会累加,sum 并不会清零
5. defer关键字
- defer关键字可以让函数在return之后执行，即使函数发生异常也会执行,为了释放资源
- 程序遇到defer关键字，不会立即执行函数体，而是将函数的调用存入一个栈中，等到函数返回时再依次执行
>[!success]- example
>```Golang
>func add(num1 int,num2 int) int {
    defer fmt.Println(num1)
    defer fmt.Println(num2)
    var result int = num1 + num2
    fmt.Println(result)
    return result
}

在输出result后，才会输出num1与num2的值
# 字符串函数 
- len() - 返回字符串的长度
>[!success]- right
>```Golang
>str := "Hello World"
> fmt.Println(len(str))
- 字符串遍历
1. 方式一 for range
>[!success]- right
>```Golang
>for i, v := range str {
>    fmt.Printf("%d:%c\n", i, v)
>}
2. 方式二 for
>[!success]- right
>```Golang
>r := []rune(str)
>for i := 0; i < len(r); i++ {
>   fmt.Println(r[i])
>}
- 字符串转整数，整数转字符串
>[!success]- right
>```Golang
>import "strconv"
>strint,_ := strconv.Atoi("123")//返回整数
>intstr:= strconv.Itoa(123)//返回字符串
>fmt.Println(strint,intstr)
- 查找与统计指定子串
>[!success]- right
>```Golang
>import "strings"
>count := strings.Count(str,"l")
>fmt.Println(count)
- 不区分大小写的字符串比较
>[!success]- right
>```Golang
>result := strings.EqualFold("hello","HELLO")//返回true
    fmt.Println(result)
- 区分大小写的字符串
>[!success]- right
>```Golang
>    fmt.Println("hello" == "HELLO")
- 返回子串在字符串第一次出现的索引值，不存在返回-1
>[!success]- right
>```Golang
>index := strings.Index(str,"W")
>fmt.Println(index)
-  字符串替换，大小写替换，拆分
>[!success]- right
>```Golang
>==替换==
str1 := strings.Replace(str,"l","L",-1)//-1表示全部替换,替换几个n就为几
>==大小写替换 ==
>fmt.Println(strings.ToLower(str))//全部小写
>fmt.Println(strings.ToUpper(str))//全部大写
>==拆分==
>arr := strings.Split(str,"l")
>```
- 字符串两边删除指定字符
>[!success]- right
>```Golang
>fmt.Println(strings.Trim(str,"H"))//删除左边l
>fmt.Println(strings.TrimRight(str,"d"))//删除右边l
>fmt.Println(strings.TrimSpace(" 6 "))//删除两边空格
>fmt.Println(strings.Trim("~6~","~"))

# 日期与时间函数[^时间戳]
- 调用日期时间函数
>[!success]- right
>```Golang
now := time.Now()
fmt.Println("Now:", now)//返回值是一个结构体，类型是time
fmt.Println("Year:", now.Year())//年份
fmt.Println("Month:", now.Month())//月份
fmt.Println("Day:", now.Day())//日期
fmt.Println("Hour:", now.Hour())//小时
fmt.Println("Minute:", now.Minute())//分钟
fmt.Println("Second:", now.Second())//秒
- 输出
>[!success]- right
>```
datestr := fmt.Sprintf("%d/%02d/%02d %02dh/%02dm/%02ds", now.Year(), now.Month(), now.Day(), now.Hour(), now.Minute(), now.Second())
fmt.Println("Date:", datestr)
datestr2 := now.Format("2006 15:04")   ==小彩蛋==
fmt.Println("Date2:", datestr2)

# 内置函数
##  len()函数
- 返回字符串的长度
## new()函数
- 分配内存
```Golang
num := new(int)//返回值是对应类型的指针,也就是int指针
*num = 100
fmt.Println(*num)
```
## make()函数
- 创建切片、map、通道
- make(Type,size) Type --> map要用
- make函数的第二个参数size，表示映射的容量，即可以存储多少个键值对，默认值为0，表示无限容量,可以省略
```Golang
b := make(map[int]string)
```